class Circle:

    def __init__(self, rand_x, rand_y, radius, img_file):
        """
        Initializes the circle object
        args:
            - rand_x : int - random starting x coordinate
            - rand_y : int - random starting y coordinate
            - radius : int - radius of circle
            - img_file : str - path to img file
        """
    
    def disappear(self):
        """
        Makes circle disappear when clicked
        args: None
        return: None
      """ 
      
      
    def circle_spawn(self, rand_x, rand_y, radius):
        """
        Spawns new circle in random location
        Args:
            - rand_x: int - random x value
            - rand_y: int - random y value
            - radius: int - radius of new circle
        return: circle_x (int), circle_y (int)
        """
